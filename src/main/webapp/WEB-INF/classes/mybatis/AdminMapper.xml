<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.supr.blog.mapper.AdminMapper">
	
	<select id="addAdmin" parameterType="Admin">
		insert into admin(username,password,isEnabled,email) values(#{username},#{password},#{isEnabled},#{email})
	</select>
	
	<select id="getAdminByEmail" resultType="Admin" parameterType="String">
		select * from admin  
		where email = #{email}
	</select>
	
	<select id="adminExists" resultType="Admin" parameterType="Admin">
		select * from admin  
		where username = #{username} and password = #{password}
	</select>
	
	<select id="getAdminByName" resultType="Admin" parameterType="String">
		select * from admin  
		where username = #{username}
	</select>
	
</mapper>
